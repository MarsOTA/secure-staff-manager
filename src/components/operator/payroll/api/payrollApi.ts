
import { fetchOperatorEvents } from './apiCore';
import { calculateHours, calculateNetHours } from './localStorageApi';

// Re-export the main function for backward compatibility
export { fetchOperatorEvents, calculateHours, calculateNetHours };
